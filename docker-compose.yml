services:
  soporte-cosechadoras:
    build: .
    container_name: soporte-cosechadoras
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped
